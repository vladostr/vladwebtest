pipeline {
    agent any
    tools{
			maven 'myMaven-3'
			jdk 'myJDK-1.8'
		 }  
    stages {
        stage('Example Build') {
            when { branch 'master' }
            steps {
                echo 'Hello, Maven'
                sh 'mvn --version'
            }
        }
        stage('Example Test') {
            when { branch 'master' }
            steps {
                echo 'Hello, JDK'
                sh 'java -version'
            }
        }
    }
}